from datetime import datetime
import requests
from distutils.log import INFO
import LoraLogger


logger = LoraLogger.logger(__name__, INFO)


def construct_data(input, datastream):
    constructed = input

    return constructed


def send(data, datastream):
    resp = None

    return resp
